using Godot;
using System;

public partial class ModelOption : StaticBody3D
{
    //todo render a viewport with the model that rotates, i think make its own world, but maybe its better to put the model itself somewhere on the map, maybe even not like this but just hats on display?

    public override void _Ready()
    {
    }

    public override void _Process(double delta)
    {
    }
}
